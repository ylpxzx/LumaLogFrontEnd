# LumaLogFrontEnd

Vue 3 + Vite frontend for LumaLog.

## Run

```bash
npm install
npm run dev
```

Default API base URL:

```text
http://localhost:8080/api
```

Override it with `.env`:

```text
VITE_API_BASE_URL=http://localhost:8080/api
```

## Build

```bash
npm run build
```
